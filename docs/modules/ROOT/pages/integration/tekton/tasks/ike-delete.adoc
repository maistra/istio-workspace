= Istio Workspace Delete Task

What is it

==  `ike-delete`
Link to docs?

=== Parameters

include::cmd:yq[args="e '.spec.params' -P -j -I4 ike-delete.yaml | jq -r '. | map(\"* `\(.name)`: \(.description).\") | .[]' || echo -e '[NOTE]\nThe task does not require any parameters.'", cwd="$PROJECT_DIR/integration/tekton/tasks/ike-delete"]

=== Results

include::cmd:yq[args="e '.spec.results' -P -j -I4 ike-delete.yaml | jq -r '. | map(\"* `\(.name)`: \(.description).\") | .[]' || echo -e '[NOTE]\nThe task does not define any results to be consumed further in the pipeline.'", cwd="$PROJECT_DIR/integration/tekton/tasks/ike-delete"]

=== Usage

include::cmd:yq[args="e '{\"params\": .params}' -I2 samples/ike-delete.yaml", cwd="$PROJECT_DIR/integration/tekton/tasks/ike-delete",block=true,format=yaml]
